# Example

Run `composer examples` after `composer install`. The same script runs from the installed ZIP using
the clean consumer's authoritative Composer loader. It constructs semantic metadata, inspects stable
finding identity and verifies the shipped corpus checksum without providing an encoding algorithm.
